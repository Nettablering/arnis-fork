[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
profile = "minimal"

# MSRV: 1.78+ (per Nettablering fork Q463 build instructions).
# `channel = "stable"` always satisfies this in practice; the explicit
# note here is for humans reading the file.
